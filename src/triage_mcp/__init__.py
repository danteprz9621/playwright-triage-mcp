"""playwright-triage-mcp: an MCP server that parses Playwright JSON test reports
and clusters failures by normalized error signature, so the same-root-cause
failures surface together for triage.
"""

__version__ = "0.1.0"
